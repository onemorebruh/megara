[x] writing site
	[x] design site
	[x] made site's structure
	[x] fix site's script so dialogue window will be just window but not another html
    [] re design site !!! at the end
[] finding file script
	[x] open search line
	[] find file by the name
	[] show file on the other window
|script|
read data form search use like that:
for i in directory:
    if data in i:
        use it in programm
    else:
        pass

[] writing file script
	[x] show the editor so user can use it
	[x] ability to write
	[] read wrtitten strings
	[] check for errors
	[] send the text to the server
|script|
send data as valuable
save data it .md file

[] change user script
    [] you see the browser's id
    [] textarea for writing browser's id
    [] button that changes id to entered one
    [] you are in system as another user
[in progress] server script
	[x] connect with server
	[] get data from site
	[] save data as file
[x] database
    [x] set database able for work
    [x] migrate django's data
    [x] plan your database
    # table !!! it is useless now
    |username(varchar(255)|password(varchar(255))|
    |6 glebov             |aboba                 |
    [x] set table so it will work with django
